import {CACHE_NAMESPACE} from './version';
const DB=CACHE_NAMESPACE; const STORE='models'; const KEY='fastconformer-full-mixed-v0.2.0';
function openDB():Promise<IDBDatabase>{return new Promise((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});}
async function get(){const db=await openDB();return new Promise<ArrayBuffer|null>((resolve,reject)=>{const r=db.transaction(STORE,'readonly').objectStore(STORE).get(KEY);r.onsuccess=()=>resolve(r.result??null);r.onerror=()=>reject(r.error);});}
async function put(data:ArrayBuffer){const db=await openDB();return new Promise<void>((resolve,reject)=>{const r=db.transaction(STORE,'readwrite').objectStore(STORE).put(data,KEY);r.onsuccess=()=>resolve();r.onerror=()=>reject(r.error);});}
export async function clearModelCache(){const db=await openDB();return new Promise<void>((resolve,reject)=>{const r=db.transaction(STORE,'readwrite').objectStore(STORE).delete(KEY);r.onsuccess=()=>resolve();r.onerror=()=>reject(r.error);});}
export async function loadModel(url:string,onProgress:(n:number,total:number)=>void){const cached=await get();if(cached){onProgress(cached.byteLength,cached.byteLength);return cached;}const res=await fetch(url);if(!res.ok)throw new Error(`Modèle HTTP ${res.status}`);if(!res.body)throw new Error('Réponse modèle sans body');const total=Number(res.headers.get('content-length')||0);const reader=res.body.getReader();const chunks:Uint8Array[]=[];let loaded=0;while(true){const {done,value}=await reader.read();if(done)break;if(value){chunks.push(value);loaded+=value.byteLength;onProgress(loaded,total);}}const out=new Uint8Array(loaded);let off=0;for(const c of chunks){out.set(c,off);off+=c.byteLength;}await put(out.buffer);return out.buffer;}
